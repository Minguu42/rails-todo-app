<%= link_to 'プロジェクトを作成', new_project_path, class: 'btn btn-info mb-5' %>
<h2 class="mb-2">プロジェクト一覧</h2>
<hr>
<% @projects.each do |project| %>
  <section>
    <h2><%= project.name %></h2>
    <p><%= project.description %></p>
    <hr>
  </section>
<% end %>
